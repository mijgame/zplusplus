#include "zplusplus.hpp"

#include <iostream>
#include <string>

functie leegte schrijf(constante karakterketting referentie tekst) {
    standaarduitvoer schuif naar links tekst;
}

moeilijk gedoe<tiepenaam T>
functie leegte doe_iets() {

}

klasse taak {};

klasse vlag {
beschermd:
    taak verwijzing t = nulverwijzing;

publiek:
    vlag(taak verwijzing t)
        : t(t) {};
};

nummer hoofdlus() {
    niet ondertekend nummer foo = 69;
    zwevend punt x = 4.f;
    karakterketting tekst = "geweldig";

    gar c = 'a';

    taak t;
    vlag v(&t);

    als ((&t) gelijk aan nulverwijzing) {

    } anders als (1 gelijk aan 2) {

    } anders als ('a' ongelijk aan 'a') {

    } anders {

    };

    nummer n = 0;
    boel conditie = onwaar;
    terwijl lus (conditie ongelijk aan waar) {
        als (n gelijk aan 15) {
            conditie = waar;
        }

        n += 1;
    }

    voor lus (nummer i = 0; i kleiner dan 10; i++ ) {
        schrijf("Hallo");
    }

    geef 0 terug;
}